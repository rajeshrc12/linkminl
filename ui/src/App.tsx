import { Button } from "@/components/ui/button";

const App = () => {
  return (
    <h1 className="text-3xl font-bold underline">
      Hello world!
      <Button>Add</Button>
    </h1>
  );
};

export default App;
